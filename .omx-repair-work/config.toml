# oh-my-codex top-level settings (must be before any [table])
notify = ["node", "/Users/noplan02/.local/lib/node_modules/oh-my-codex/dist/scripts/notify-hook.js"]
model_reasoning_effort = "xhigh"
developer_instructions = "You have oh-my-codex installed. AGENTS.md is your orchestration brain and the main orchestration surface. Use skill/keyword routing like $name plus spawned role-specialized subagents for specialized work. Codex native subagents are available via .codex/agents and may be used for independent parallel subtasks within a single session or team pane. Skills are loaded from installed SKILL.md files under .codex/skills, not from native agent TOMLs. Use workflow skills via $name when explicitly invoked or clearly routed by AGENTS.md. Treat installed prompts as narrower internal execution surfaces under AGENTS.md authority, even when user-facing docs prefer $name keywords."
model_context_window = 1000000
model_auto_compact_token_limit = 900000

model_provider = "codex"
model = "gpt-5.4"
disable_response_storage = true

[model_providers]
[model_providers.codex]
name = "codex"
wire_api = "responses"
requires_openai_auth = true
base_url = "https://code.ddsst.online/v1"
[features]
multi_agent = true
child_agents_md = true
codex_hooks = true

[env]
USE_OMX_EXPLORE_CMD = "1"

[agents]
max_threads = 6
max_depth = 2

[mcp_servers.omx_state]
type = "stdio"
command = "node"
args = ["/Users/noplan02/.local/lib/node_modules/oh-my-codex/dist/mcp/state-server.js"]
enabled = true
startup_timeout_sec = 5

[mcp_servers.omx_memory]
type = "stdio"
command = "node"
args = ["/Users/noplan02/.local/lib/node_modules/oh-my-codex/dist/mcp/memory-server.js"]
enabled = true
startup_timeout_sec = 5

[mcp_servers.omx_code_intel]
type = "stdio"
command = "node"
args = ["/Users/noplan02/.local/lib/node_modules/oh-my-codex/dist/mcp/code-intel-server.js"]
enabled = true
startup_timeout_sec = 10

[mcp_servers.omx_trace]
type = "stdio"
command = "node"
args = ["/Users/noplan02/.local/lib/node_modules/oh-my-codex/dist/mcp/trace-server.js"]
enabled = true
startup_timeout_sec = 5

[projects."/Volumes/ZO/ZO.DESIGN"]
trust_level = "trusted"
# ============================================================
# End oh-my-codex